// frontend/postcss.config.js
module.exports = {
  plugins: {
    // Tailwind CSS plugin for processing Tailwind directives
    tailwindcss: {},
    // Autoprefixer plugin to automatically add vendor prefixes to CSS properties
    // for broader browser compatibility.
    autoprefixer: {},
  },
};
